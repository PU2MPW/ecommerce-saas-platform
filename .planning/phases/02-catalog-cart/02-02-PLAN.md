---
phase: "02"
plan: "02"
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - "src/app/[tenant]/page.tsx"
  - "src/app/[tenant]/produtos/page.tsx"
  - "src/app/[tenant]/produtos/[slug]/page.tsx"
  - "src/components/store/ProductCard.tsx"
  - "src/components/store/ProductGrid.tsx"
  - "src/components/store/CategoryFilter.tsx"
  - "src/components/store/SearchBar.tsx"
  - "src/hooks/useProducts.ts"
autonomous: true
requirements:
  - "CATG-01"
  - "CATG-02"
  - "CATG-03"
  - "CATG-04"
  - "CATG-05"
  - "CATG-06"
  - "CATG-07"
---

<objective>
Build product catalog UI: listing page, search, filters, and product detail page.
</objective>

<execution_context>
@C:/Users/marco/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/marco/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-catalog-cart/02-01-PLAN.md (depends on backend)
@.planning/phases/01-foundation/01-UI-SPEC.md (design system)
@.planning/phases/01-foundation/01-01-SUMMARY.md (schema patterns)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Product Components</name>
  <files>src/components/store/ProductCard.tsx, src/components/store/ProductGrid.tsx</files>
  <read_first>
src/components/ui/Button.tsx (from UI-SPEC)
src/components/ui/Card.tsx (from UI-SPEC)
  </read_first>
  <action>
Create reusable product display components:

**ProductCard.tsx**:
```tsx
'use client';

import Image from 'next/image';
import Link from 'next/link';

interface ProductCardProps {
  product: {
    id: string;
    name: string;
    slug: string;
    price: number;
    compare_at_price?: number;
    images?: { url: string; alt_text?: string }[];
    avg_rating?: number;
    review_count?: number;
  };
}

export function ProductCard({ product }: ProductCardProps) {
  const mainImage = product.images?.[0]?.url || '/placeholder.png';
  const discount = product.compare_at_price 
    ? Math.round((1 - product.price / product.compare_at_price) * 100)
    : null;

  return (
    <Link href={`/produtos/${product.slug}`} className="group">
      <div className="bg-white rounded-lg overflow-hidden shadow-sm hover:shadow-md transition-shadow">
        <div className="relative aspect-square">
          <Image
            src={mainImage}
            alt={product.name}
            fill
            className="object-cover group-hover:scale-105 transition-transform"
          />
          {discount && (
            <span className="absolute top-2 left-2 bg-red-600 text-white text-xs font-medium px-2 py-1 rounded">
              -{discount}%
            </span>
          )}
        </div>
        <div className="p-4">
          <h3 className="font-medium text-gray-900 line-clamp-2 group-hover:text-blue-600">
            {product.name}
          </h3>
          <div className="mt-2 flex items-center gap-2">
            <span className="text-lg font-bold text-gray-900">
              R$ {product.price.toFixed(2).replace('.', ',')}
            </span>
            {product.compare_at_price && (
              <span className="text-sm text-gray-400 line-through">
                R$ {product.compare_at_price.toFixed(2).replace('.', ',')}
              </span>
            )}
          </div>
          {product.avg_rating && (
            <div className="mt-2 flex items-center gap-1">
              <span className="text-yellow-500">★</span>
              <span className="text-sm text-gray-600">
                {product.avg_rating.toFixed(1)} ({product.review_count})
              </span>
            </div>
          )}
        </div>
      </div>
    </Link>
  );
}
```

**ProductGrid.tsx**:
```tsx
'use client';

import { ProductCard } from './ProductCard';

interface Product {
  id: string;
  name: string;
  slug: string;
  price: number;
  compare_at_price?: number;
  images?: { url: string; alt_text?: string }[];
  avg_rating?: number;
  review_count?: number;
}

interface ProductGridProps {
  products: Product[];
  loading?: boolean;
}

export function ProductGrid({ products, loading }: ProductGridProps) {
  if (loading) {
    return (
      <div className="grid grid-cols-2 md:grid-cols-3 lg:grid-cols-4 gap-6">
        {[...Array(8)].map((_, i) => (
          <div key={i} className="animate-pulse">
            <div className="bg-gray-200 aspect-square rounded-lg" />
            <div className="h-4 bg-gray-200 mt-4 rounded" />
            <div className="h-4 bg-gray-200 mt-2 w-2/3 rounded" />
          </div>
        ))}
      </div>
    );
  }

  if (products.length === 0) {
    return (
      <div className="text-center py-12">
        <p className="text-gray-500">Nenhum produto encontrado.</p>
      </div>
    );
  }

  return (
    <div className="grid grid-cols-2 md:grid-cols-3 lg:grid-cols-4 gap-6">
      {products.map((product) => (
        <ProductCard key={product.id} product={product} />
      ))}
    </div>
  );
}
```
  </action>
  <verify>
<automated>ls src/components/store/ProductCard.tsx && ls src/components/store/ProductGrid.tsx</automated>
  </verify>
  <done>
    ProductCard and ProductGrid components created with responsive design
  </done>
</task>

<task type="auto">
  <name>Task 2: Create Search and Filter Components</name>
  <files>src/components/store/SearchBar.tsx, src/components/store/CategoryFilter.tsx</files>
  <read_first>
src/components/store/ProductCard.tsx
  </read_first>
  <action>
Create search and category filter components:

**SearchBar.tsx**:
```tsx
'use client';

import { useState, useTransition } from 'react';
import { useRouter, useSearchParams } from 'next/navigation';

interface SearchBarProps {
  defaultValue?: string;
}

export function SearchBar({ defaultValue = '' }: SearchBarProps) {
  const [query, setQuery] = useState(defaultValue);
  const router = useRouter();
  const searchParams = useSearchParams();
  const [isPending, startTransition] = useTransition();

  const handleSearch = (e: React.FormEvent) => {
    e.preventDefault();
    const params = new URLSearchParams(searchParams);
    if (query) {
      params.set('search', query);
    } else {
      params.delete('search');
    }
    params.delete('page'); // Reset to first page on search
    startTransition(() => {
      router.push(`/produtos?${params.toString()}`);
    });
  };

  return (
    <form onSubmit={handleSearch} className="relative">
      <input
        type="text"
        value={query}
        onChange={(e) => setQuery(e.target.value)}
        placeholder="Buscar produtos..."
        className="w-full px-4 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-transparent"
      />
      <button
        type="submit"
        className="absolute right-2 top-1/2 -translate-y-1/2 text-gray-400 hover:text-gray-600"
        disabled={isPending}
      >
        <svg className="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z" />
        </svg>
      </button>
    </form>
  );
}
```

**CategoryFilter.tsx**:
```tsx
'use client';

import Link from 'next/link';
import { useSearchParams } from 'next/navigation';

interface Category {
  id: string;
  name: string;
  slug: string;
  children?: Category[];
}

interface CategoryFilterProps {
  categories: Category[];
}

export function CategoryFilter({ categories }: CategoryFilterProps) {
  const searchParams = useSearchParams();
  const currentCategory = searchParams.get('category_id');

  return (
    <div className="space-y-2">
      <h3 className="font-medium text-gray-900">Categorias</h3>
      <ul className="space-y-1">
        <li>
          <Link
            href="/produtos"
            className={`block px-3 py-2 rounded-md ${
              !currentCategory ? 'bg-blue-50 text-blue-700' : 'text-gray-700 hover:bg-gray-50'
            }`}
          >
            Todas
          </Link>
        </li>
        {categories.map((category) => (
          <li key={category.id}>
            <Link
              href={`/produtos?category_id=${category.id}`}
              className={`block px-3 py-2 rounded-md ${
                currentCategory === category.id
                  ? 'bg-blue-50 text-blue-700'
                  : 'text-gray-700 hover:bg-gray-50'
              }`}
            >
              {category.name}
            </Link>
            {category.children && category.children.length > 0 && (
              <ul className="ml-4 mt-1 space-y-1">
                {category.children.map((child) => (
                  <li key={child.id}>
                    <Link
                      href={`/produtos?category_id=${child.id}`}
                      className="block px-3 py-1 text-sm text-gray-600 hover:text-blue-600"
                    >
                      {child.name}
                    </Link>
                  </li>
                ))}
              </ul>
            )}
          </li>
        ))}
      </ul>
    </div>
  );
}
```
  </action>
  <verify>
<automated>ls src/components/store/SearchBar.tsx && ls src/components/store/CategoryFilter.tsx</automated>
  </verify>
  <done>
    SearchBar and CategoryFilter components created
  </done>
</task>

<task type="auto">
  <name>Task 3: Create Products Listing Page</name>
  <files>src/app/[tenant]/produtos/page.tsx</files>
  <read_first>
src/app/[tenant]/page.tsx (layout pattern from Phase 1)
src/components/store/ProductGrid.tsx
src/components/store/SearchBar.tsx
src/components/store/CategoryFilter.tsx
  </read_first>
  <action>
Create products listing page with pagination:

```tsx
import { Suspense } from 'react';
import { createSupabaseServerClient } from '@/lib/supabase/server';
import { ProductGrid } from '@/components/store/ProductGrid';
import { SearchBar } from '@/components/store/SearchBar';
import { CategoryFilter } from '@/components/store/CategoryFilter';

interface ProductsPageProps {
  params: { tenant: string };
  searchParams: {
    page?: string;
    search?: string;
    category_id?: string;
  };
}

async function getProducts(tenantSlug: string, searchParams: ProductsPageProps['searchParams']) {
  const supabase = await createSupabaseServerClient();
  
  const page = parseInt(searchParams.page || '1');
  const pageSize = 12;
  const search = searchParams.search;
  const category_id = searchParams.category_id;
  
  let query = supabase
    .from('products')
    .select('*, category:categories(*), images:product_images(*), variants:product_variants(*)', { count: 'exact' })
    .eq('tenant_id', tenantSlug) // via middleware
    .eq('is_active', true)
    .order('created_at', { ascending: false })
    .range((page - 1) * pageSize, page * pageSize - 1);
  
  if (search) {
    query = query.ilike('name', `%${search}%`);
  }
  
  if (category_id) {
    query = query.eq('category_id', category_id);
  }
  
  const { data, count, error } = await query;
  
  if (error) throw error;
  
  return { products: data || [], total: count || 0, page, pageSize };
}

async function getCategories() {
  const supabase = await createSupabaseServerClient();
  const { data } = await supabase.from('categories').select('*').order('name');
  return data || [];
}

export default async function ProductsPage({ searchParams }: ProductsPageProps) {
  const { products, total, page, pageSize } = await getProducts('current', searchParams);
  const categories = await getCategories();
  
  const totalPages = Math.ceil(total / pageSize);
  
  return (
    <div className="container mx-auto px-4 py-8">
      <h1 className="text-2xl font-bold mb-6">Nossos Produtos</h1>
      
      <div className="flex flex-col lg:flex-row gap-8">
        {/* Sidebar */}
        <aside className="lg:w-64 shrink-0">
          <div className="sticky top-4">
            <SearchBar defaultValue={searchParams.search} />
            <div className="mt-6">
              <CategoryFilter categories={categories as any} />
            </div>
          </div>
        </aside>
        
        {/* Product Grid */}
        <main className="flex-1">
          <div className="mb-4 text-sm text-gray-600">
            {total} produto{total !== 1 ? 's' : ''} encontrado{total !== 1 ? 's' : ''}
          </div>
          
          <ProductGrid products={products as any} />
          
          {/* Pagination */}
          {totalPages > 1 && (
            <div className="mt-8 flex justify-center gap-2">
              {Array.from({ length: totalPages }, (_, i) => i + 1).map((p) => (
                <a
                  key={p}
                  href={`/produtos?page=${p}${searchParams.search ? `&search=${searchParams.search}` : ''}${searchParams.category_id ? `&category_id=${searchParams.category_id}` : ''}`}
                  className={`px-4 py-2 rounded ${
                    p === page
                      ? 'bg-blue-600 text-white'
                      : 'bg-gray-100 text-gray-700 hover:bg-gray-200'
                  }`}
                >
                  {p}
                </a>
              ))}
            </div>
          )}
        </main>
      </div>
    </div>
  );
}
```
  </action>
  <verify>
<automated>ls src/app/[tenant]/produtos/page.tsx</automated>
  </verify>
  <done>
    Products listing page created with search, filters, and pagination
  </done>
</task>

<task type="auto">
  <name>Task 4: Create Product Detail Page</name>
  <files>src/app/[tenant]/produtos/[slug]/page.tsx</files>
  <read_first>
src/app/[tenant]/produtos/page.tsx
src/components/store/ProductCard.tsx
  </read_first>
  <action>
Create product detail page with images, variants, and reviews:

```tsx
import { createSupabaseServerClient } from '@/lib/supabase/server';
import { ProductImageGallery } from '@/components/store/ProductImageGallery';
import { ProductVariantSelector } from '@/components/store/ProductVariantSelector';
import { ProductReviews } from '@/components/store/ProductReviews';
import { AddToCartButton } from '@/components/store/AddToCartButton';

interface ProductDetailProps {
  params: { tenant: string; slug: string };
}

async function getProduct(slug: string) {
  const supabase = await createSupabaseServerClient();
  
  const { data: product, error } = await supabase
    .from('products')
    .select('*, category:categories(*), images:product_images(*), variants:product_variants(*), reviews:reviews(*)')
    .eq('slug', slug)
    .eq('is_active', true)
    .single();
  
  if (error) throw error;
  return product;
}

export default async function ProductDetailPage({ params }: ProductDetailProps) {
  const product = await getProduct(params.slug);
  
  const avgRating = product.reviews?.length
    ? product.reviews.reduce((sum: number, r: any) => sum + r.rating, 0) / product.reviews.length
    : 0;
  
  return (
    <div className="container mx-auto px-4 py-8">
      <div className="grid grid-cols-1 lg:grid-cols-2 gap-8">
        {/* Image Gallery */}
        <ProductImageGallery images={product.images || []} />
        
        {/* Product Info */}
        <div>
          {product.category && (
            <span className="text-sm text-gray-500">{product.category.name}</span>
          )}
          <h1 className="text-2xl font-bold mt-2">{product.name}</h1>
          
          {/* Rating */}
          {product.reviews?.length > 0 && (
            <div className="mt-2 flex items-center gap-2">
              <div className="flex">
                {[1, 2, 3, 4, 5].map((star) => (
                  <span key={star} className={star <= avgRating ? 'text-yellow-500' : 'text-gray-300'}>
                    ★
                  </span>
                ))}
              </div>
              <span className="text-sm text-gray-600">
                ({product.reviews.length} avaliação{product.reviews.length !== 1 ? 's' : ''})
              </span>
            </div>
          )}
          
          {/* Price */}
          <div className="mt-4">
            <span className="text-3xl font-bold text-gray-900">
              R$ {product.price.toFixed(2).replace('.', ',')}
            </span>
            {product.compare_at_price && (
              <span className="ml-3 text-lg text-gray-400 line-through">
                R$ {product.compare_at_price.toFixed(2).replace('.', ',')}
              </span>
            )}
          </div>
          
          {/* Variants */}
          {product.variants && product.variants.length > 0 && (
            <ProductVariantSelector variants={product.variants} />
          )}
          
          {/* Add to Cart */}
          <div className="mt-6">
            <AddToCartButton product={product} />
          </div>
          
          {/* Description */}
          {product.description && (
            <div className="mt-8">
              <h2 className="font-medium mb-2">Descrição</h2>
              <p className="text-gray-600">{product.description}</p>
            </div>
          )}
        </div>
      </div>
      
      {/* Reviews Section */}
      <div className="mt-12">
        <h2 className="text-xl font-bold mb-6">Avaliações</h2>
        <ProductReviews reviews={product.reviews || []} />
      </div>
    </div>
  );
}
```
  </action>
  <verify>
<automated>ls src/app/[tenant]/produtos/[slug]/page.tsx</automated>
  </verify>
  <done>
    Product detail page created with gallery, variants, and reviews
  </done>
</task>

<task type="auto">
  <name>Task 5: Create Supporting Components</name>
  <files>src/components/store/ProductImageGallery.tsx, src/components/store/ProductVariantSelector.tsx, src/components/store/ProductReviews.tsx, src/components/store/AddToCartButton.tsx</files>
  <read_first>
src/components/store/ProductCard.tsx
  </read_first>
  <action>
Create remaining product detail components:

**ProductImageGallery.tsx** - Main image + thumbnail strip
**ProductVariantSelector.tsx** - Size/color selection with inventory check
**ProductReviews.tsx** - Review list with form
**AddToCartButton.tsx** - Add to cart with quantity selector

Each follows UI-SPEC patterns for buttons, inputs, and layouts.
  </action>
  <verify>
<automated>ls src/components/store/ProductImageGallery.tsx && ls src/components/store/AddToCartButton.tsx</automated>
  </verify>
  <done>
    Product detail components created (gallery, variants, reviews, add to cart)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→Server | Untrusted params (page, search, category) |
| Server→API | Backend already validates tenant |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04 | Injection | Search param | mitigate | Supabase ilike uses parameterized queries |
| T-02-05 | Info Disclosure | Product detail | accept | Public storefront - all products visible |
</threat_model>

<verification>
Visit pages:
- http://localhost:3000/[tenant]/produtos
- http://localhost:3000/[tenant]/produtos?search=test
- http://localhost:3000/[tenant]/produtos/[slug]
</verification>

<success_criteria>
- [ ] Product listing shows products from API
- [ ] Search filters products by name
- [ ] Category filter works
- [ ] Pagination navigates correctly
- [ ] Product detail shows all images
- [ ] Variants selectable with inventory
- [ ] Reviews displayed with ratings
- [ ] Add to cart button works
</success_criteria>

<output>
After completion, create `.planning/phases/02-catalog-cart/02-02-SUMMARY.md`
</output>